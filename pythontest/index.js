$( document ).ready(function() {
	
	
   /* sUrl = 'http://52.30.239.185/search/scup/wup'
    $.ajax(sUrl, {
        complete: function(p_oXHR, p_sStatus){
			console.log("well boi");
            oData = $.parseJSON(p_oXHR.responseText);
			console.log(oData);
			$('#well').text(oData);
	}});*/
});
